pipeline {
    agent any

    stages {
        stage ('Build') {

            steps {
                steps {
                 sh 'mvn clean compile'
                      }
                  }
          }
    }
}
